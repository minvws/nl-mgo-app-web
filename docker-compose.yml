services:
    mgo:
        container_name: mgo
        build:
            context: .
            dockerfile: Dockerfile
            args:
                - PORT=${PORT-8000}
                - OIDC_AUTHORITY
                - LOAD_URL
                - DVA_URL
        ports:
            - ${PORT:-8000}:8080
