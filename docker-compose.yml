services:
    mgo:
        container_name: mgo
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - ${PORT:-8000}:8000
        volumes:
            - .:/app
            - /app/node_modules
            - /app/apps/react/node_modules
            - /app/packages/fhir-client/node_modules
            - /app/packages/fhir-data/node_modules
            - /app/packages/react-ui/node_modules
            - /app/packages/tailwind/node_modules
