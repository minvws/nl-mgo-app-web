volumes:
    hapi-data:

services:
    mgo:
        container_name: mgo
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - ${PORT:-8000}:8000

    hapi:
        container_name: hapi
        image: smartonfhir/hapi-5:r3-smart
        ports:
            - 8080:8080
