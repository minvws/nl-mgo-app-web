# The environments all require a connection through the VPN
# or a basic auth header - which is what these credentials are for.
# All environments (LOAD / DVP / WEB) share the same credentials.
BASIC_AUTH_USER=******
BASIC_AUTH_PASSWORD=******

APP_ENVIRONMENT=local

ENABLE_ACCESSIBILITY_CHECKS=true
PW_EXTRA_PROJECTS=true
