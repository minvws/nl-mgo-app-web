// @ts-check

const { fontFamily, fontSize, fontWeight, lineHeight, letterSpacing } = require('./typography.cjs');
const { colors } = require('./colors.cjs');

module.exports = {
    extend: {},
    colors,
};
