// Made available in its own file to avoid circular dependencies

/**
 * Missing values are represented by null.
 * This is done so we can compare the output with the output that the mobile apps use.
 */
export const EMPTY_VALUE = null;
