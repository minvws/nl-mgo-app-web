import { Canvas, Meta } from '@storybook/blocks';
import { Typography } from './docs.stories';

<Meta title="Typography" />

# Typography

<br />

## Fonts

The following fonts are defined in the `@minvws/mgo-tailwind` theme. They are loaded via the `@minvws/mgo-tailwind/fonts.css` file.

<Canvas of={Typography} />
